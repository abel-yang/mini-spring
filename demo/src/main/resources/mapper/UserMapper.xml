<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.minis.test.jdbc.User">
    <select id="getUserInfo" parameterType="java.lang.Integer" resultType="com.minis.test.jdbc.User">
        select id, name
        from customer
        where id = ?
    </select>
</mapper>